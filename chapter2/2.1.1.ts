let firstName = 'Seo';

firstName.length();

// 1. 코드를 읽고 firstName이라는 변수를 이해
// 2. 초기 값이 'Seo' 이므로 firstName이 string 타입이라는 결론
// 3. firstName의 .length 맴버를 함수처럼 호출하는 코드를 확인
// 4. .length 는 함수가 아니기 때문에 함수 처럼 사용해서 에러 표시

export {};